{
  "name": "omniscitus",
  "version": "0.8.0",
  "private": true,
  "description": "The maintenance layer your codebase deserves.",
  "scripts": {
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "seed": "node scripts/seed-omniscitus.js",
    "build:demo": "node scripts/build-demo-birdview.js"
  },
  "engines": {
    "node": ">=18"
  }
}
